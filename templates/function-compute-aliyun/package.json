{
  "name": "fun-app",
  "private": true,
  "scripts": {
    "build": "rimraf ./dist && tsc --build tsconfig.json",
    "start": "fun local start",
    "eslint": "eslint --config .eslintrc.js --ext .ts --quiet ./src"
  },
  "dependencies": {
    "raw-body": "3.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.18.5",
    "eslint": "^8.56.0",
    "eslint-config-nstarter": "^4.1.0",
    "nstarter-tsconfig": "0.5.0",
    "typescript": "~5.9.2"
  }
}
